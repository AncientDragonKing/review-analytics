# review(留言)-analytics(分析)
data = []
count = 0
with open('reviews.txt', 'r') as f:
	for line in f:
		data.append(data)
		count += 1
		if count % 1000 == 0:
			print(len(data))
print('檔案讀取玩了，總共有', len(data), '筆留言')